This is a helper function.

Features:

1 - Database connection via PDO
2 - Retrieving the user's real IP address when behind a proxy
3 - User redirection redirect("auth/login");
4 - Generating a random CSRF token
5 - Redirect the user to the previous page

And other useful features
